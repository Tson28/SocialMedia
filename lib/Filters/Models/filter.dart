class Filters {
  String imagePath;
  String filterPath;
  String nameFilter; // Thêm thuộc tính nameFilter

  Filters({
    required this.filterPath,
    required this.imagePath,
    required this.nameFilter, // Thêm vào hàm khởi tạo
  });
}
